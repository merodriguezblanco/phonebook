<div class="thumbnail">
  <img src="{{contact.photo}}" alt="{{contact.fullName}}">
  <div class="caption">
    <h3>{{contact.fullName}}</h3>
    <p>{{contact.email}}</p>
    <p>{{contact.address}}</p>
    <p>{{contact.phone}}</p>
  </div>
</div>
